# V2-01 验收证据

> 历史命令说明：本文中的 PowerShell、Shell、deployment Profile 和核心 Kustomize 命令只记录当时验收事实，相关入口已经删除，不得作为当前操作入口；当前部署使用 `agentxctl`，仓库门禁使用 `cargo xtask check`，集群验收使用 `pytest tests/e2e`。

> 历史环境快照：本文中的 `agentx-v2-*` Namespace 仅用于记录当时的验收环境，不代表当前部署规范。

基线日期：2026-08-13。最终 Kubernetes 证据 Run ID 为 `20260813t0021`，原始结果位于 [`artifacts/v2/20260813t0021/v2-01/`](../../../artifacts/v2/20260813t0021/v2-01/README.md)。本阶段只证明分域基础设施、空库 Schema、凭据/网络隔离和 Control Redis 清理成立；没有宣称 V2-02 Bundle 发布链路、Runtime Internal HTTP Handler 或控制面离线执行完成。

## 交付与边界

- `migrations/control/0001_initial.sql`：96 张 Control 业务表和独立 SQLx History；`migrations/runtime/0001_initial.sql`：72 张 Runtime 业务表和独立 SQLx History。物理表数量包含 39 个 Split replacement；133 个原表中 131 个有 Control/Runtime replacement，2 个 Delete 表不创建。
- `migrations/observability/0001_initial.sql`：独立 ClickHouse History 和按租户、Execution、时间排序的 `ReplacingMergeTree` Trace 表。Migration Runner 使用独立锁表串行化并发版本检查，结束后释放锁。
- `agentx-v2-ops` 提供 `agentx-migrate`、`agentx-doctor` 和 `agentx-bootstrap` 三域入口；入口拒绝携带非本域数据库、Redis、对象存储或 ClickHouse 环境变量。Doctor 检查版本、Migration History、必需表、最小权限、实际 TLS/协议状态和对象存储，Runtime 额外检查独占 Redis；MySQL Doctor 从实例读取 `max_connections` 并拒绝超过 70% 的连接池预算。
- `deployment/v2alpha1` Profile、四个 Namespace、分域 ServiceAccount/Secret、默认拒绝 NetworkPolicy、两个 MySQL、Runtime Redis、ClickHouse 和三 Bucket MinIO IAM 已形成可部署清单。
- `scripts/deploy-v2.ps1` 在部署入口强制校验两个 MySQL Endpoint/账号、三域对象存储 Bucket/账号、ClickHouse Query/Migrate 账号和 70% 连接预算，随后负责随机 Secret、部署、Migration、Bootstrap 双重重放和 Doctor；`scripts/v2-01-e2e.ps1` 负责唯一临时 Namespace、断言、证据和失败清理。
- Platform Control 的 Redis Settings、Client、后台 Relay/Projector、健康检查、队列读取和 Cargo 依赖已删除。运行队列查询在 V2-05 Runtime Query 接入前明确返回 `unknown`。
- Runtime Object Reference 契约固定为 `domain/tenant_id/object_id/content_hash/size`，契约 Schema 和 Internal OpenAPI 已同步生成。

## Kubernetes 动态结果

Docker Desktop Kubernetes `docker-desktop` 使用以下临时 Namespace：

```text
agentx-v2-01-control-20260813t0021
agentx-v2-01-runtime-20260813t0021
agentx-v2-01-observability-20260813t0021
agentx-v2-01-deps-20260813t0021
```

连续 Run 结果：

- MySQL 8.4.11 Control/Runtime 使用独立 StatefulSet、Service、PVC、随机 Root/App/Migrate Secret 和账号。应用账号 DML 成功、DDL 失败；另一域账号不存在且访问失败。
- Control、Runtime、Observability Migration 首次运行成功；三域各两个并发 Replay Job 均成功。两套 MySQL SQLx History 与 ClickHouse History 保持独立。
- Control Bootstrap 幂等创建系统 Tenant、测试用户和 Workflow 服务身份；Runtime 只创建 Tenant Admission；三域 Bootstrap 各连续执行两次成功，没有跨库 SQL Seed。Control Schema 额外验证 Tenant/User/Workflow 同域外键成立，跨租户 Workflow Owner 写入被拒绝。
- 三域 Doctor 全部通过：Control/Runtime MySQL 8.4.11、ClickHouse 25.3.14.14、Migration History、必需表、最小权限、三个对象存储 Bucket，以及 Runtime Redis 均可用。Control/Runtime 连接池分别为 60/151（39%）和 100/151（66%），均低于实例 70% 上限；本地 Profile 使用明文协议，Doctor 明确报告 TLS 未加密，没有伪报 TLS 成功。
- Runtime Schema 动态验证 Deployment Head CAS、幂等键冲突拒绝、非法 Execution 状态拒绝、Admission Epoch `0` 拒绝，以及领域状态与 Outbox 同事务回滚原子性。
- Control→冻结 Runtime Internal API 的 HTTP 8080 白名单正向可达；Runtime→Model/MCP/RAG/Memory/Sandbox 五类已标记 Provider 正向可达；Control、Runtime、Observability 三域公开入口仅允许带入口标签的客户端访问。Control→Runtime MySQL/Redis/ClickHouse、Runtime→Control MySQL、Observability→Runtime MySQL、Runtime→未授权 Provider 和无入口标签客户端均被 NetworkPolicy 拒绝。
- ClickHouse Query 账号可 SELECT Trace 表但不能 DDL；相同 `event_id` 的两个版本经 `FINAL` 查询只返回新版本；Observability Secret 不含 Runtime MySQL Credential。
- Control、Runtime、Observability 三个 MinIO 账号只能读写自己的 Bucket，跨域读取和写入均失败；Doctor 与 E2E 探针对象均删除。
- Runtime Redis 完成认证读写；删除 Redis PVC 后空库重建，重建前标记键不存在且 StatefulSet 恢复 Ready。
- 成功和失败演练均执行四 Namespace 清理。最终 Run 实际将开发 `agentx` 的 11 个 Deployment 和 4 个 StatefulSet 缩到 0，结束时恢复 1/1 Ready；清理后的 `agentx-v2-01-*` Namespace 集合为空。
- Secret 值未写入证据；证据只保留 Secret Key 数量、镜像 ID、Profile、Doctor JSON、资源快照、断言和时间线。

## 静态和回归门禁

- `agentx-boundary-check` 通过，Control/Runtime 初始 Schema 与 133 项处置目录精确相等，2 个 Delete 表未创建。
- Cargo、SQL、Env/Secret 和 NetworkPolicy 正负 Fixture 通过；V2 Profile 的 32 种 bundled/external 模式组合均完成声明式 Manifest dry-run 渲染，共享 Namespace、MySQL Endpoint/User、Bucket/User、ClickHouse User 和超出 70% MySQL 连接预算的负向 Fixture 均被拒绝。
- `scripts/check.ps1` 于 2026-08-13 完整通过，耗时 863.4 秒；其中包含 `cargo fmt --all -- --check`、全 Workspace Clippy `-D warnings`、`cargo test --workspace`、Platform/Gateway/Node/Runtime 契约漂移、Web 回归、部署/Profile/供应链检查和全部 Kustomize 渲染。全 Workspace 测试包含数据库/Testcontainers 长用例。
- Web lint 为 0 error、5 个既有 warning；45 个测试文件/181 项测试通过；Production Build 通过。
- `kubectl kustomize deploy/k8s/v2` 渲染不包含 Secret、占位密码、通用 `AGENTX_MYSQL_*` 或 `AGENTX_REDIS_*`。
- `apps/web/src` 零差异；浏览器 API、页面、路由和样式未修改。

后续 V2-02 必须基于这些空库和凭据边界实现 Bundle Prepare/Activate 及基础垂直切片，不能重新引入共享 MySQL 或 Control Redis。
