# V2-08A 本地功能总收口验收证据

> 历史命令说明：本文中的 PowerShell、Shell、deployment Profile 和核心 Kustomize 命令只记录当时验收事实，相关入口已经删除，不得作为当前操作入口；请使用 `agentx-deploy`、`agentx-check` 与 `pytest tests/e2e`。

> 历史环境快照：本文中的 `agentx-v2-*` Namespace 仅用于记录当时的验收环境，不代表当前部署规范。

## 1. 结论

V2-08A 已完成，完整产品认证 Run 为 `wf5n-20260820011355`。该 Run 从空 Control/Runtime MySQL、Runtime Redis、ClickHouse 和三域 Bucket 启动 V2 三域，未部署任何 V1 服务；公共 API、产品闭环、控制面离线、Runtime 故障和本地性能回归全部通过，测试结束后三个临时 Namespace 均已删除。Trace 语义化、Artifact 下载和 Redis Worker 自恢复改造后的最新专项回归 Run 为 `traceart-r8-20260821`，结果同样为 `passed`。

本证据只关闭本地功能收口。V2 总计划仍为 `in_progress`；`V2S-006`、正式容量与两小时稳定性、生产 TLS/PITR/RPO/RTO、gVisor/Kata、Role 级权限隔离、Cosign/Attestation 和多租户攻击矩阵继续由 V2-08B 验证。

## 2. 最终环境与证据入口

- Run ID：`wf5n-20260820011355`
- 总结：`artifacts/v2/wf5n-20260820011355/v2-08/08a/summary.json`
- 部署配置：`artifacts/v2/wf5n-20260820011355/v2-08/08a/v2-full-local.json`
- 性能：`artifacts/v2/wf5n-20260820011355/v2-08/08a/performance.json`
- 渲染清单：`artifacts/v2/wf5n-20260820011355/v2-08/08a/render.yaml`
- JUnit：同目录下 `api-first-junit.xml`、`workflow4-junit.xml`、`control-ui-junit.xml` 和 `product-closure-junit.xml`
- 三域 Doctor：同目录下 `doctor-control.log`、`doctor-runtime.log` 和 `doctor-observability.log`

临时 Namespace：

```text
agentx-v2-08-control-wf5n-20260820011355
agentx-v2-08-runtime-wf5n-20260820011355
agentx-v2-08-deps-wf5n-20260820011355
```

Docker Desktop 本地 Kubernetes 使用 16 核/16 GiB 规格；八类应用均以两个副本启动。Observability 与 ClickHouse 使用 Runtime Namespace。`summary.json` 的 `status` 与 `cleanup.status` 均为 `passed`，`cleanup.errors` 为空。

## 3. 功能与故障结果

最终 Run 的 11 个场景全部为 `passed`：

1. Control `1..7`、Runtime `1..8`、Observability `1..2` Migration 历史完整；Trace 语义契约已破坏性并入 Observability `0002`。
2. 空 V2 三域部署成功，渲染和集群工作负载均无 V1 服务。
3. API-first Bootstrap 与正式 API 创建业务对象通过。
4. Workflow 5.0 回归通过。
5. Control UI、IAM 与资源授权会签回归通过。
6. Studio、Application、Runtime、治理、Trace、安全删除等产品闭环通过。
7. Web 与 Platform Control 离线时，已发布 Runtime Invocation 继续完成。
8. Runtime MySQL 停机时新写明确返回 `503`，恢复后无伪成功。
9. ClickHouse 停机不改变 Workflow Runtime 终态。
10. Runtime Redis `FLUSHALL` 并重建 Pod 后，执行从 Runtime MySQL 权威事实恢复并收敛。
11. 本地性能回归通过。

V2-08A 最终拓扑复验与 V2-02～06 的专项 E2E 共同关闭 E2E-V2-001～011 的本地功能和故障语义；多副本竞争与滚动扩缩容沿用并在本轮回归 V2-06A 证据。生产规模的滚动兼容、容量和稳定性结论不在本证据范围内。

## 4. API 与 V1 删除

公共 Platform API 处置结果：

```text
totalPaths=151
implementedPaths=151
migrationRequiredPaths=0
deletionAllowed=true
```

`platform-control` 已成为 `/api/v1` 与 `openapi/platform-api.json` 的唯一实现和生成源。V2 `runtime-gateway` 继续唯一承载 `/gateway/v1` 并生成 `openapi/trigger-gateway.json`。

已物理删除 V1 `platform-api`、`trigger-gateway`、`trace-writer`、`workflow-coordinator`、旧 `workflow-worker`、旧 `sandbox-manager`、`agentx-runtime-rpc`、`agentx-infrastructure-legacy`、共享 `migrations/mysql`、V1 Kubernetes Stack/Profile/Nginx/脚本分支及仅服务 V1 的 Fixture。边界策略的 Legacy 例外为零；V2 Provider Fixture 已迁入 `deploy/k8s/v2/e2e/runtime-providers`。

## 5. 本地性能回归

```text
并发 Execution             50
Node Attempt              200
SSE 连接                  100
稳定运行                  30 分钟
Invocation 接受 p95       123.98 ms
```

门禁期间没有非预期 Pod Restart、连接池耗尽、Lease 卡死、重复终态或本次测试产生的活跃 Command/Outbox/Reservation/Hold 残留。该结果只证明本地明显性能退化门禁通过，不代表生产容量上限或 SLO。

## 6. 自动化门禁

以下最终门禁通过：

```text
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo run --quiet -p agentx-boundary-check -- check .
scripts/check.ps1
scripts/v2-07-profile-tests.ps1 -SkipWebSourceBaseline
kubectl kustomize deploy/k8s/v2
kubectl kustomize deploy/k8s/v2/e2e/runtime-providers
git diff --check
```

`scripts/check.ps1` 同时通过 Contracts/OpenAPI/前端类型漂移、Profile/Kustomize、2000 行、Web lint、185 项 Web 测试和 production build。`git diff --check` 只有 Windows 工作树的 LF/CRLF 提示，没有空白错误。

## 7. 清理与无效 Run

最终 Run 的三个临时 Namespace、临时集群资源、端口转发和本地 OpenSandbox 进程均已清理，开发副本已恢复。证据中不保存 API Key、Vault Token、Credential 或 Webhook Secret 明文。

`20260818-final`、`20260818-final2`、`20260818-final3` 和 `20260818-final4` 分别暴露并促成修复 V1 Fixture 引用、Windows 休眠、Prometheus Adapter 启动竞态以及 Runtime/Redis 恢复路径；这些 Run 的清理均通过，但不作为完成证据。

## 8. Trace 语义化与 Artifact 专项回归

最新有效专项 Run：`traceart-r8-20260821`。该 Run 从空 Control/Runtime MySQL、Runtime Redis、ClickHouse 和三域 Bucket 全量重建三域，应用使用两个副本；Control UI 专项因本轮没有改动而跳过，API-first、Workflow 4、Studio/Trace 产品闭环、故障矩阵和性能回归均重新执行。

证据入口：

- 总结：`artifacts/v2/traceart-r8-20260821/v2-08/08a/summary.json`
- 性能：`artifacts/v2/traceart-r8-20260821/v2-08/08a/performance.json`
- JUnit：同目录下 `api-first-junit.xml`、`workflow4-junit.xml`、`product-closure-junit.xml`、`trace-degraded-junit.xml` 和 `trace-recovered-junit.xml`
- 三域 Doctor：同目录下 `doctor-control.log`、`doctor-runtime.log` 和 `doctor-observability.log`

13 个场景全部 `passed`，12 项 Playwright 测试为 0 failure、0 error、0 skipped。专项覆盖并关闭：

1. Model system/user 消息顺序、标准 `text` 输出及 `Model.text → End` 引用。
2. 超过 16 KiB 的真实 Provider 响应外置为 `contentRef`；Studio 通过 Execution-scoped API 下载 JSON，内容包含 Fixture Marker 且不包含 Credential、Authorization 或测试 Secret。
3. 默认节点视图在 ClickHouse 停机时继续展示 Runtime MySQL 权威输入输出；ClickHouse 恢复后高级 Trace 自动重试并补齐。
4. Studio Runtime Panel、独立执行详情和 Node Inspector 共用 Artifact 下载契约。
5. Redis `FLUSHALL + Pod 重建` 后，Worker 在队列读取错误/超时时按 capability 重建连接和 Consumer Group；依赖错误不再误触发 liveness，后续执行无需 Pod 重启即可恢复。

专项性能结果：

```text
并发 Execution             50
Node Attempt              200
SSE 连接                  100
稳定运行                  30 分钟
Invocation 接受 p95       165.64 ms
```

性能窗口内稳定性 Workflow 全部成功，Runtime Pod restartCount 无新增，Command/Outbox/Reservation/Hold 无活跃残留。`summary.status=passed`、`cleanup.status=passed`、`cleanup.errors=[]`；三个临时 Namespace 均已删除，`agentx-control` 和 `agentx-runtime` 开发 Deployment 已全部恢复为 `1/1`。

专项改造后的本地门禁再次通过 `cargo fmt --all -- --check`、Workspace 严格 Clippy、Workspace 全量测试、V2 Boundary Check、Web 53 个测试文件/218 项测试、Web production build 以及 E2E TypeScript 编译；前后端源文件均未超过 2000 行，根目录原型 `demo.html` 已删除。

`traceart-r6-20260821` 暴露 Redis 故障重试被错误判为 liveness stalled，`traceart-r7-20260821` 进一步暴露半开队列连接未主动重建；两次 Run 的功能与清理场景均通过，但性能门禁失败，因此只作为缺陷发现证据，不作为完成证据。

## 9. 状态关闭

- `V2C-001～004`：`done`
- `V2C-007` 本地完整产品等价部分：`done`
- V2-08A：`done`
- `V2C-005` 容量版、`V2C-006` 最终生产发布审查：`planned`，延期到 V2-08B
- V2-06、V2-07、V2-08 与 V2 总计划：仍为 `in_progress`
