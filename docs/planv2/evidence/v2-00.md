# V2-00 验收证据

> 历史环境快照：本文中的 `agentx-v2-*` Namespace 仅用于记录当时的验收环境，不代表当前部署规范。

基线日期：2026-08-12。最终证据 Run ID 为 `20260812T113914252Z`。V2-00 只证明契约和代码边界成立；没有宣称双库、Internal HTTP Handler、控制面离线运行或三面 Kubernetes 已完成，这些仍属于 V2-01～08。

原始结果保存在 [`artifacts/v2/20260812T113914252Z/v2-00/`](../../../artifacts/v2/20260812T113914252Z/v2-00/README.md)。该目录记录命令、退出码、计数、前端哈希、E2E JUnit 来源和 Kubernetes 清理/恢复结果。

## 交付物

- `agentx-runtime-contracts`：Compiled IR、Execution Spec Bundle、Work Package、Command/Event、Publish/Receipt、Export/Query、RS256 Service/Delegation JWT。
- `schemas/runtime-v1/*.schema.json` 与 `openapi/runtime-internal-v1.json`：独立于浏览器 OpenAPI；所有顶层 Wire Family 使用数字版本 `1`，版本 `2` 和未知字段被拒绝。
- `agentx-control-infrastructure`：Control MySQL、Credential/Secret Provider、Control Artifact、Control Outbox 和类型化 Control S3 构造；这些 Platform Control 路径不再通过 Legacy 构造。
- `agentx-runtime-infrastructure`：Gateway、Coordinator、Worker 和 Sandbox 的 Runtime 构造入口。
- `agentx-infrastructure-legacy`：冻结 V1 实现；直接依赖、Rust Import、SQL、动态 SQL 和 NetworkPolicy 例外均精确到文件、Owner 和删除阶段，禁止增长。
- `agentx-mysql-lease`：数据库时间、Owner、Fencing、LeaseLost、静态 SQL 模板与双面 MySQL Harness。
- `table-disposition.json`：133 张表恰好一次，当前分布为 Control 58、Runtime 34、Split 39、Delete 2。
- 服务运行契约、版本窗口、Claim 指南、V1 删除台账和机器可执行边界策略。

## 契约、边界和 Lease 证据

```powershell
& scripts/generate-v2-table-disposition.ps1
cargo run --quiet -p agentx-boundary-check -- check .
cargo test -p agentx-boundary-check
cargo test -p agentx-runtime-contracts
cargo test -p agentx-mysql-lease --test mysql_protocol
cargo run --quiet -p agentx-runtime-contracts --bin generate-contracts -- schemas/runtime-v1 openapi/runtime-internal-v1.json
```

最终结果：

- 表目录 `133` 条、唯一表名 `133` 个、`TBD=0`、缺失 Writer `0`。
- 边界正向 Fixture 通过；Cargo、SQL、Env/Secret、NetworkPolicy 五类真实临时仓库负向 Fixture 全部通过生产检查入口并取得非零拒绝结果。
- 冻结 Legacy 基线为：直接 Cargo 依赖 3；Rust Import 22 文件/63 次；SQL 386 项/855 次；动态 SQL 78 项/83 次；边界 Token 0；NetworkPolicy 文件 2。
- Contracts 共 12 项测试，覆盖 RFC 8785 JCS、数组顺序、数字规范化、SHA-256、独立 Ed25519 Key、篡改、未知字段、数字版本拒绝、RS256 双 `kid`、Issuer/Audience/Role/Scope、租户和 Delegation 限制。
- MySQL Lease 共 4 项测试；集成 Harness 覆盖 Control/Runtime 临时表、20 路并发 Claim、DB UTC 时间、Pod 快/慢本地时钟无关性、过期接管、Claimant 强退、旧 Owner/Token、Fail 和重复 Complete。

## 最终全仓门禁

在仓库根目录执行：

```powershell
& scripts/check.ps1
```

最终退出码为 `0`。该入口通过：

- Cargo Metadata/SQL/Env/Secret/NetworkPolicy/表目录边界检查，以及五类负向 Fixture。
- `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets -- -D warnings` 和 `cargo test --workspace`。
- 当前 MySQL/ClickHouse Migration 空库、幂等和升级测试。
- Node、Platform、Gateway 和 Runtime Internal Schema/OpenAPI 重新生成零漂移；Platform/Gateway 浏览器 TypeScript 契约零漂移。
- `pnpm lint:web`（5 个既有 warning、0 error）、45 个 Web 测试文件共 181 项测试，以及 `pnpm build:web`。
- 576 个部署 Profile 组合、7 个 Release 镜像检查，以及 Full/E2E/LightRAG/Mem0 Kustomize 渲染。

`git diff --check` 通过。检查 4940 个前后端/脚本源文件，没有文件超过 2000 行；最大文件为 `services/platform-api/src/main.rs`，1931 行。

## Kubernetes E2E 与环境恢复

先重建并装载当前工作树的六个后端 release 镜像，再运行：

```powershell
& scripts/e2e.ps1 -Namespace agentx-v2-00-20260812-final -OnlySuite m6 -SkipBuild
```

最终 Run ID `20260812T113914252Z` 的两个 JUnit Suite 共 5 项测试全部通过，0 failure、0 error，总测试时间 143.900066 秒：

- M2.1 Control Plane：1/1；真实浏览器完成 Workflow/资源创建、保存、版本化和发布。
- M6 Runtime/Studio：4/4；覆盖本地 Builtin 执行、Studio 调试执行与查询、版本发布、双 Agent/Application 调用结果，以及 500/1000 节点画布性能。

验收期间发现并关闭两个编排缺口：Fixture Job 现在继承唯一临时 Namespace；已有受管 Secret 会补齐 V2-00 类型化 Control/Runtime MySQL、Redis 和 S3 Key。E2E 画布连线 Harness 也增加了有界重试，未修改前端生产源码。

测试后确认：

- 临时 Namespace `agentx-v2-00-20260812-final` 不存在。
- 开发 Namespace `agentx` 的 11 个 Deployment 和 4 个 StatefulSet 均恢复为 `1/1` Ready。
- E2E 新建的 OpenSandbox 实例清理完成。

## 前端、安全与范围

- `git diff -- apps/web/src` 和 `git diff --cached -- apps/web/src` 均为空；249 个前端生产文件哈希清单摘要为 `f3cd3adff76a6a1bf91ab21415546b4ca3e1617d41a3bf5ab968c1c5d6ddd8cb`。
- 浏览器 API、页面、路由、布局、组件、文案、设计令牌和样式未变。
- RSA 私钥只存在于测试 Fixture，未写入部署清单或证据；证据不包含 Secret 明文。
- MySQL Fixture 由 Testcontainers 创建和回收，不修改正式 Migration。

本证据只支持 V2A-001～010 和 V2-00 `done`。V2-01 的两个独立空 MySQL、Runtime 独占 Redis、正式 V2 Schema，以及 V2-02 的 Bundle 发布链路和本阶段只生成契约而未实现的 Runtime Internal HTTP Handler，仍为后续任务。
