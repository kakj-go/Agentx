# P3-00 验收证据

状态：`done`（2026-08-24，Asia/Shanghai）。P3-00 仅包含审计、Spike、契约、测试和文档，没有接入生产 Agent路径。

## 交付物

| 任务 | 交付物 | 结果 |
|---|---|---|
| P3A-001 | `p3-00/current-code-map.md`、`deletion-ledger.json` | 当前入口和每项旧实现均有唯一处置 |
| P3A-002 | `p3-00/reference-audit.md`、Pi校验脚本 | Commit/Tree/Package/MIT/关键 Blob固定 |
| P3A-003 | `fixtures/agent-core` 15 个版本化案例 | Schema校验、能力覆盖和版本绑定通过 |
| P3A-004 | `crates/agentx-agent-core` | 独立 Port驱动 Loop/Context/Compaction/Tool Spike通过 |
| P3A-005 | `operation.rs`、恢复测试、ADR-P3-002/004 | safe/never/unknown/fencing收敛 |
| P3A-006 | Rust类型、`schemas/agent-core-v1`、OpenAPI、ADR | 生成和漂移测试通过；Process Session只冻结契约 |
| P3A-007 | 本证据、P3-01输入、Plan3状态同步 | 阶段门禁关闭 |

## 已执行命令

```text
uv run --frozen python scripts/plan3/verify_pi_reference.py --source <temp-pi-checkout>
  PASS: fixed commit / package 0.84.2 / MIT / 8 critical blobs / 15 fixtures

uv run --frozen python scripts/plan3/verify_pi_reference.py --source <temp-pi-checkout> --run-upstream-tests
  PASS: 63 upstream Loop/Compaction/Context/read/write/edit tests

cargo test -p agentx-agent-core
  PASS: 4 operation + 8 driver + 1 fixture + 4 contract + 1 schema drift tests

cargo run --quiet -p agentx-agent-core --bin generate-agent-core-contracts -- schemas/agent-core-v1 openapi/agent-core-contracts-v1.json
  PASS: 8 JSON Schemas + OpenAPI 3.1 component draft
  OpenAPI SHA-256: dc98deb6e656a8d44a194fd89eefd36ae4b845fa933d23fb96d9a6cf2a62c5fa

cargo test --workspace -- --test-threads=1
  PASS: 全 Workspace Rust unit/integration/doc tests

cargo clippy --workspace --all-targets -- -D warnings
cargo run --quiet -p agentx-boundary-check -- check .
git diff --check
  PASS: Clippy、Core基础设施边界、仓库边界和空白检查
```

## 事实与限制

- 当前 Git工作树未提交，因此证据 Commit记为 `working-tree`；参考 Pi Commit为 `a69bef789bc95abf0acee16f7b4660b70b650bb9`。
- Spike使用 Fake Model/Tool、内存 State、固定 Clock/Budget；不声称真实 Provider、MySQL、OpenSandbox或 Kubernetes E2E已完成。
- stdio MCP Process Session只有机器契约和 ADR，没有生产进程管理实现；归 P3C-001～003。
- 上游 Bash测试在当前 Windows主机因 WSL缺少 `/bin/bash` 未纳入默认门禁；Bash源码/Schema已审计，真实隔离执行与差分测试归 P3T-001/P3T-004。脚本提供 `--run-bash-tests` 在具备环境时重跑。
- `agentx-agent-core` 没有被生产 Worker依赖。当前生产 `execute_agent` 保持不变。
- P3-01～P3-07仍为 `planned`；P3-00完成不等于 Plan3整体完成。

## P3-01 进入门禁

固定参考、行为矩阵、Fixture、Core Port、完整 Operation State、恢复决策、Cargo边界、Definition/Manifest/Bundle草案、Agent可选 Sandbox、stdio必选 Sandbox、Process Session契约、Grant闭包、容量/升级 ADR和删除台账均已有可定位交付物，无 `TBD` 处置项。
