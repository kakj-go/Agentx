# P6-01 首个垂直切片证据

JSON Mapper模板通过页面真实文件选择导入。Playwright使用React Flow Handle真实鼠标拖动建立`Start → acme.json_mapper → Exit`，修改插件Panel、保存、运行、查询节点输出、展开Trace瀑布并加载自定义`Mapped items`视图。

本地正式安装 Playwright 主链路通过：`1 passed (21.9s)`。临时 Kubernetes Run `2dbcbb6379` 的插件聚焦入口为 `7 passed in 403.36s`，成功后三个临时 Namespace 均删除；完整 Run `21cefd9702` 为 `26 passed in 1263.87s`。

Worker从Execution Snapshot中的冻结 Runtime Artifact 引用运行；Artifact 已由Control复制到Runtime OSS，本地缓存每次按大小和摘要校验，损坏时只重取同一不可变对象。Worker不访问Control包配置或源码目录。容器内实测Node为`v24.20.0`，Runner文件位于`/opt/agentx/plugin-runner/runner.mjs`。
