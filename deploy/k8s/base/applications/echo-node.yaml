apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo-node
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: echo-node
  template:
    metadata:
      labels:
        app.kubernetes.io/name: echo-node
    spec:
      containers:
        - name: echo-node
          image: agentx/echo-node:dev
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: agentx-config
            - secretRef:
                name: agentx-secrets
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
---
apiVersion: v1
kind: Service
metadata:
  name: echo-node
spec:
  selector:
    app.kubernetes.io/name: echo-node
  ports:
    - name: http
      port: 8080
      targetPort: http
