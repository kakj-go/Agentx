apiVersion: apps/v1
kind: Deployment
metadata:
  name: trace-writer
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: trace-writer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: trace-writer
    spec:
      containers:
        - name: trace-writer
          image: agentx/trace-writer:dev
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: agentx-config
            - secretRef:
                name: agentx-secrets
          ports:
            - name: health
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health

