apiVersion: batch/v1
kind: Job
metadata:
  name: m3-fixture
  namespace: agentx-e2e
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: m3-fixture
    spec:
      restartPolicy: Never
      containers:
        - name: fixture
          image: agentx/m3-fixture:dev
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: agentx-config
            - secretRef:
                name: agentx-secrets
