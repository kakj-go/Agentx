apiVersion: v1
kind: Service
metadata: { name: sandbox-manager, namespace: {{ .Values.global.namespaces.runtime }} }
spec:
  selector: { app.kubernetes.io/name: sandbox-manager }
  ports: [{ name: http, port: 8080, targetPort: http }]
