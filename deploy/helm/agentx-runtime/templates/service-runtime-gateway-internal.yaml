apiVersion: v1
kind: Service
metadata: { name: runtime-gateway-internal, namespace: {{ .Values.global.namespaces.runtime }} }
spec:
  selector: { app.kubernetes.io/name: runtime-gateway }
  ports: [{ name: http, port: 8080, targetPort: http }]
