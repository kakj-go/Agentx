apiVersion: v1
kind: Service
metadata: { name: workflow-runtime-metrics, namespace: {{ .Values.global.namespaces.runtime }} }
spec:
  clusterIP: None
  selector: { app.kubernetes.io/name: workflow-runtime }
  ports: [{ name: metrics, port: 9092, targetPort: metrics }]
