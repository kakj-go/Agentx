apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: sandbox-manager, namespace: {{ .Values.global.namespaces.runtime }} }
spec: { minAvailable: 1, selector: { matchLabels: { app.kubernetes.io/name: sandbox-manager } } }
