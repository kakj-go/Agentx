apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: runtime-provider-egress, namespace: {{ .Values.global.namespaces.dependencies }} }
spec:
  podSelector: { matchLabels: { agentx.io/runtime-provider: allowed } }
  policyTypes: [Egress]
  egress:
    - to: [{ podSelector: { matchLabels: { agentx.io/runtime-provider: allowed } } }]
      ports:
        - { protocol: TCP, port: 8090 }
        - { protocol: TCP, port: 5432 }
    - to: [{ namespaceSelector: {}, podSelector: { matchLabels: { k8s-app: kube-dns } } }]
      ports: [{ protocol: UDP, port: 53 }, { protocol: TCP, port: 53 }]
