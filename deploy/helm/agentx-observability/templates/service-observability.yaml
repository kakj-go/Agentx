apiVersion: v1
kind: Service
metadata: { name: observability, namespace: {{ .Values.global.namespaces.runtime }} }
spec:
  type: ClusterIP
  selector: { app.kubernetes.io/name: observability }
  ports: [{ name: http, port: 8080, targetPort: http }]
