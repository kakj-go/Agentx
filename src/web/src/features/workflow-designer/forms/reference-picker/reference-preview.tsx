import { useTranslation } from 'react-i18next'
import type { ReferenceEntry } from './reference-types'

export function ReferencePreview({ entry }: { entry?: ReferenceEntry }) {
  const { t } = useTranslation()
  if (!entry) return <div className="p-3 text-xs text-muted-foreground">{t('studio.references.select')}</div>
  return <div className="space-y-3 p-3 text-xs"><div><div className="text-[10px] uppercase text-muted-foreground">{t('studio.references.path')}</div><code className="mt-1 block break-all text-[11px] text-foreground">{entry.path}</code></div>{entry.description && <p className="rounded bg-muted/50 p-2 text-[11px] text-muted-foreground">{entry.description}</p>}<dl className="grid grid-cols-[72px_1fr] gap-y-1 text-[11px]"><dt className="text-muted-foreground">{t('studio.references.type')}</dt><dd>{entry.type ?? t('studio.references.unknown')}</dd><dt className="text-muted-foreground">{t('studio.references.cardinality')}</dt><dd>{entry.cardinality ?? t('studio.references.single')}</dd><dt className="text-muted-foreground">{t('studio.references.nullable')}</dt><dd>{entry.nullable ? t('studio.references.yes') : t('studio.references.no')}</dd>{entry.scope && <><dt className="text-muted-foreground">{t('studio.references.scope')}</dt><dd>{entry.scope}</dd></>}{entry.sensitive && <><dt className="text-muted-foreground">{t('studio.references.sensitive')}</dt><dd className="text-danger">{t('studio.references.notInsertable')}</dd></>}</dl>{entry.conversionNote && <p className="rounded bg-primary/10 p-2 text-[11px] text-primary">{entry.conversionNote}</p>}{entry.disabledReason && <p className="rounded bg-warning/10 p-2 text-[11px] text-warning">{entry.disabledReason}</p>}{entry.example !== undefined && !entry.sensitive && <div><div className="text-[10px] uppercase text-muted-foreground">{t('studio.references.example')}</div><pre className="mt-1 max-h-20 overflow-auto bg-canvas p-2 text-[10px]">{JSON.stringify(entry.example, null, 2)}</pre></div>}</div>
}
