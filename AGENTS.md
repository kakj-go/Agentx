# 项目说明
目前项目还在开发阶段，一些重构和修改不用考虑历史数据的兼容，可以直接清理

## 仓库目录

主要工程目录固定为 `src/`、`contracts/`、`deploy/`、`tools/`、`tests/`、`docs/`。产品源码位于 `src/web`、`src/services`、`src/crates` 与 `src/plugins`；CLI 和仓库自动化位于 `tools`；共享协议位于 `contracts`；数据库初始化文件位于 `deploy/migrations`。不要重新引入旧根目录或旧路径兼容入口。

本地证据统一写入 `.local/artifacts`，发布包写入 `.local/dist`，临时镜像归档写入 `.local/tmp/images`。`target`、`node_modules` 和 `.venv` 保留工具默认位置。完整映射与历史证据定位规则见 `docs/plan/repository-layout.md`。

## 约束

1. 先阅读本文件和与任务相关的 `docs/` 文档；总索引见 `docs/README.md`。
2. 前端文件代码行数不要超过2000行
3. 后端文件代码行数不要超过2000行
4. 前端代码要保证 ui，风格，样式，组件保持一致，前端应该是统一的组件库，样式库这样来保证整体风格，字体，大小，ui，排列一致性
5. 修改前检查 Git 工作树。已有修改默认属于用户，不得覆盖、回退或删除无关内容。
7. 如果实现需要改变已确定的架构边界，先说明原因和影响，并同步更新相关设计文档。
8. 整体功能需要有 E2E 端到端自动化测试，整体流程测试具体测试可以 k8s 创建临时使用的 namespace 来实现，测试完成之后删除，测试期间可以先将 agentx 这个命名空间停止所有服务来缓解资源压力
9. 不保留向后兼容。过时的直接删，别加兼容层、别写migration、别留fallback。
10. 选能满足当前需求的最简单实现。不要预防性抽象，不要多此一举的配置层。
12. 组件保持模块化，关注点分离。 优先用成熟的、有人维护的库。没有明确理由别自己重写。
13. 先翻项目里已有的依赖能做什么，再考虑加新包或自己写。别上来就假设库里没有。
14. 架构决策往长了做。不接受"先这样以后再换"的临时方案。
15. 先看成熟产品怎么解决同一个问题，用已验证的模式，别从零发明。
16. 当前企业门户仅支持 web 桌面端作为正式产品体验和验收范围；移动端导航、窄屏布局和移动端专属交互不属于本项目当前版本的实现与 E2E 验收目标。

## 脚本与自动化

1. 仓库内的跨平台自动化、测试编排、验收、部署辅助、数据检查和临时验证脚本统一使用 Python，不得新增 `.ps1`、`.bat` 或 `.cmd` 文件。
2. Kubernetes 系统级 E2E 以 `pytest tests/e2e` 作为唯一编排入口；浏览器业务操作继续使用现有 TypeScript Playwright，由 Python 负责环境准备、进程管理、调用 Playwright、证据收集和最终清理。
3. 新增脚本前先复用现有 Rust `agentxctl`、`cargo xtask` 和 `tests/e2e` Python 基础设施；Python 只负责自动化编排，不复制 Rust 中已有的部署、业务或契约逻辑。
4. Python 脚本使用项目现有 `uv`、`pyproject.toml` 和测试依赖，不单独建立依赖安装方式；优先使用标准库和仓库已有依赖。
5. 修改到仍在使用的 PowerShell 脚本时，应直接迁移为 Python 并删除旧 `.ps1`，不保留兼容包装脚本或双入口。
6. 上述限制针对提交到仓库的自动化脚本，不限制开发者或 AI 在 Windows 终端中临时执行 PowerShell 命令，也不适用于 Playwright 测试、前端构建脚本、容器入口等由所属生态决定语言的代码。
