import { ShieldX } from 'lucide-react';import { Link } from 'react-router-dom';import { useTranslation } from 'react-i18next';import { Button } from '../../shared/ui/button';
export function ForbiddenPage(){const{t}=useTranslation();return <div className="grid min-h-screen place-items-center bg-background"><div className="text-center"><ShieldX className="mx-auto size-12 text-danger"/><h1 className="mt-5 text-2xl font-semibold">{t('auth.forbidden.title')}</h1><p className="mt-2 text-sm text-muted-foreground">{t('auth.forbidden.description')}</p><Button asChild className="mt-6"><Link to="/">{t('pages.notFound.back')}</Link></Button></div></div>}
